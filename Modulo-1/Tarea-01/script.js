function calcular(){
    var pes = (document.convertidor.pesos.value) * 1;

    var tot = pes / 16;

    document.convertidor.dolar.value = tot;

}

function calcular(){
    var pes = (document.convertidor.pesos.value) * 1;

    var tot = pes / 16; 

    document.convertidor.dolar.value = tot;
}